{
  "$schema": "https://opencode.ai/config.json",
  "lsp": true,
  "command": {
    "spec": {
      "description": "Crea especificaciones de pantallas y funcionalidades",
      "template": "Carga y sigue la skill spec en .agents/skills/spec/SKILL.md.\n\nFeature: $ARGUMENTS"
    }
  },
  "permission": {
    "external_directory": {
      "/tmp/**": "allow"
    },
    "bash": "allow"
  },
  "references": {
    "db-schema": {
      "path": "../info-database",
      "description": "Schema de la base de datos del sistema open-daycare. Incluye tablas, columnas y referencias a crear. Esto no esta implementado en la base de datos, es solo para referencia"
    }
  },
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp@latest"],
      "enabled": true
    }
  }
}
